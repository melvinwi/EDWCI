# design test artefact			
# ARTEFACT: VCustomer_tests			
# DESCRIPTION: Test view			
DATASTORE_SQL	VIEW_SQL	TEST	DESCRIPTION
select count(*) from [schema].DimCustomer	select count(*) from [schema].VCustomer 	source.length.should.equal(destination.length)	size